[request_definition]
r = sub, res, act, env

[policy_definition]
p = act, res, cond，eff

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = eval(p.sub_rule) && r.obj == p.obj && r.act == p.act
